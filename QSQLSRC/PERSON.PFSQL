CREATE TABLE PERSON (
    ID INT NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY
                (START WITH 1, INCREMENT BY 1),
    NAME VARCHAR(40),
    SURNAME VARCHAR(40),
    CARD VARCHAR(9),
    EMAIL VARCHAR(30)
);
